<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title %></title>
     <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <META name="keywords" content=<%= @seo_keywords%> />

    <%= stylesheet_link_tag    'portfolioitems', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- start of nav -->
    
    <%= render 'shared/portfolio_nav' %>
    <!-- end of nav -->

    <!-- start of masthead -->
   

    <%= render 'shared/portfolio_masthead' unless logged_in?(:site_admin)%>
    <!-- start of masthead -->

    <%= yield %>
      <!-- start of footer -->
     <%= render 'shared/portfolio_footer'%>
      <!-- end of footer -->
    
      

    <%= set_source_helper("portfolio item")%>
  </body>
</html>